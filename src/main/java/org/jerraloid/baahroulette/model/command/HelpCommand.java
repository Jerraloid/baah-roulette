package org.jerraloid.baahroulette.model.command;

public class HelpCommand {
	//gives a list of all the commands
	/*if(command.equalsIgnoreCase("help") || command.equalsIgnoreCase("commands")) {
		StringBuilder sb = new StringBuilder();
		for(int i = 0; i < Ref.COMMANDS.length; i++) {
			sb.append(Ref.COMMANDS[i] + "\n");
		}
		
		sendMessage(messageChannel, "Bot Commands", sb.toString());
	}*/
}
